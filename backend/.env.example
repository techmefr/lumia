DATABASE_URL=postgresql+asyncpg://lumia:changeme@localhost/lumia
REDIS_URL=redis://localhost:6379

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174

JWT_SECRET=change-me-to-a-32-byte-random-secret
SECRET_ENCRYPTION_KEY=change-me-to-a-fernet-key

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=change-me
SMTP_PASSWORD=change-me
SMTP_FROM_ADDRESS=noreply@example.com

MINIFLUX_BASE_URL=http://localhost:8090
MINIFLUX_USERNAME=admin
MINIFLUX_PASSWORD=change-me

# Optional: without it, translation goes through the account's own AI key, or is unavailable.
DEEPL_API_KEY=
